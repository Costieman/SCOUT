# Architecture Decision Records

ADRs record material technical decisions, their context, alternatives, consequences, and status. Accepted ADRs govern implementation unless superseded by a later accepted ADR.

ADR identifiers are repository-wide unique. The numeric identifier in the filename must match the identifier in the document heading; an existing identifier is never reused for a different decision. The architecture acceptance suite enforces these rules in CI.
