# A+B versioning boundary v0.1

Evidence reports identify the evaluation version separately from any future canonical dataset version. Changes to tolerance, identity links, provider semantics, or promotion rules require a new reproducible evidence run rather than silently rewriting prior classifications.
