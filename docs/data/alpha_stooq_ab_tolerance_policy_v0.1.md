# A+B tolerance policy v0.1

Price and volume tolerances are explicit run inputs. Zero remains the default for evidence generation. Any non-zero tolerance used for acceptance must be justified empirically and versioned; tolerance is not a mechanism for hiding systematic provider differences.
