# A+B source roles v0.1

Alpha Vantage is labelled provider A and Stooq provider B only for stable report semantics. The labels do not encode a truth hierarchy. A-only and B-only observations receive symmetric review requirements unless a later, evidence-backed policy explicitly establishes a field-specific preference.
