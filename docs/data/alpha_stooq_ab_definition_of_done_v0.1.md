# A+B definition of done v0.1

The A+B code path is done when CI passes and the implementation is merged. The A+B *data acceptance* is done only after representative live runs have been reviewed and the residual gap/disagreement profile is understood. These are deliberately separate milestones.
