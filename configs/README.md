# Configuration

This directory reserves the configuration hierarchy defined by the project specifications. The human-authored serialization format and typed validation library remain open pre-code decisions, so Phase 0B deliberately does not invent configuration schemas.
